#include <stdio.h>

main()
{
    /** 
     * HINT: some compilation warnings, some turn into space characters,
     * some are unknown to the compiler and have the backlash stripped,
     * some may give compilation errors (such as \x, used with hex characters). 
     */
    printf("Experimenting with backlashed characters: \g \1 \m \v \c \n");
}
